// src/store/index.js
// Cấu hình store (ví dụ với Redux Toolkit)
import { configureStore } from '@reduxjs/toolkit';

export const store = configureStore({
  reducer: {}, // Thêm reducer tại đây
});
